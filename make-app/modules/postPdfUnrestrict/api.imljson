{
  "url": "/v1.0/pdf/unrestrict",
  "method": "POST",
  "qs": {
    "format": "{{parameters.format}}",
    "file_id": "{{parameters.file_id}}",
    "destroy": "{{parameters.destroy}}",
    "output": "{{parameters.output}}",
    "file_password": "{{parameters.file_password}}",
    "user_password": "{{parameters.user_password}}",
    "owner_password": "{{parameters.owner_password}}",
    "webhook_url": "{{parameters.webhook_url}}",
    "webhook_failure_notification": "{{parameters.webhook_failure_notification}}"
  },
  "body": {
    "file": "{{parameters.file}}"
  },
  "response": {
    "output": "{{body}}"
  }
}
