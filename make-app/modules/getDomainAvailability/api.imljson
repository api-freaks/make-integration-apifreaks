{
  "url": "/v1.0/domain/availability",
  "method": "GET",
  "qs": {
    "format": "{{parameters.format}}",
    "domain": "{{parameters.domain}}",
    "source": "{{parameters.source}}"
  },
  "response": {
    "output": "{{body}}"
  }
}
