{
  "url": "/v1.0/commodity/time-series",
  "method": "GET",
  "qs": {
    "symbols": "{{parameters.symbols}}",
    "startDate": "{{parameters.startDate}}",
    "endDate": "{{parameters.endDate}}"
  },
  "response": {
    "output": "{{body}}"
  }
}
