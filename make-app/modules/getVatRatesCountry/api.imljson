{
  "url": "/v1.0/vat/rates/country",
  "method": "GET",
  "qs": {
    "country": "{{parameters.country}}",
    "state": "{{parameters.state}}"
  },
  "response": {
    "iterate": "{{body}}",
    "output": "{{item}}",
    "limit": "{{parameters.limit}}"
  }
}
