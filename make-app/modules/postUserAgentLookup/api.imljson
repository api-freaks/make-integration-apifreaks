{
  "url": "/v1.0/user-agent/lookup",
  "method": "POST",
  "body": {
    "uaStrings": "{{parameters.uaStrings}}"
  },
  "response": {
    "iterate": "{{body}}",
    "output": {
      "value": "{{item}}"
    },
    "limit": "{{parameters.limit}}"
  }
}
