{
  "url": "/v2.0/geolocation/lookup",
  "method": "GET",
  "qs": {
    "format": "{{parameters.format}}",
    "ip": "{{parameters.ip}}",
    "lang": "{{parameters.lang}}",
    "fields": "{{parameters.fields}}",
    "excludes": "{{parameters.excludes}}",
    "include": "{{parameters.include}}"
  },
  "response": {
    "output": "{{body}}"
  }
}
