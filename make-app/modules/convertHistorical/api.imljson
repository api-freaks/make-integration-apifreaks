{
  "url": "/v1.0/currency/converter/historical/prices",
  "method": "GET",
  "qs": {
    "from": "{{parameters.from}}",
    "to": "{{parameters.to}}",
    "amount": "{{parameters.amount}}",
    "date": "{{parameters.date}}"
  },
  "response": {
    "output": "{{body}}"
  }
}
