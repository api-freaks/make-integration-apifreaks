{
  "url": "/v1.0/zipcode/search/city",
  "method": "GET",
  "qs": {
    "city": "{{parameters.city}}",
    "country": "{{parameters.country}}",
    "state_name": "{{parameters.stateName}}"
  },
  "response": {
    "iterate": "{{body.codes}}",
    "output": {
      "value": "{{item}}"
    },
    "limit": "{{parameters.limit}}"
  },
  "pagination": {
    "qs": {
      "page": "{{pagination.page}}"
    },
    "condition": "{{body.current_page < body.total_pages}}"
  }
}
